name: Daily BRAC Scraper

on:
  schedule:
    - cron: '0 */6 * * *' # Runs every 6 hours
  workflow_dispatch: # Allows you to click a button to run it manually

jobs:
  scrape_and_process:
    runs-on: ubuntu-latest
    permissions:
      contents: write # Allows the Action to push the JSON file back to the repo

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run Scraper and Gemini Processing
      env:
        GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
      run: python main.py

    - name: Commit and Push JSON Data
      run: |
        git config --local user.email "github-actions[bot]@users.noreply.github.com"
        git config --local user.name "github-actions[bot]"
        git add bracu_data.json
        # The || echo part prevents the Action from crashing if no new data was found
        git commit -m "Auto-update BRACU data [skip ci]" || echo "No changes to commit"
        git push
